name: "Conformance Gate"
description: >
  Policy-driven conformance gate for Rust microservices. Runs two engines per
  service: a deterministic static engine (source + Dockerfile regex/logic) and
  an AI engine for judgement rules (envelope, architecture, validation). Every
  rule, severity and the AI prompt derive from a single policy YAML
  (policy-path). Evaluates one service or every service in a directory.

inputs:
  service-path:
    description: "Relative path to a single Rust service to evaluate (from repo root)"
    required: false
  services-dir:
    description: "Relative path to a directory containing multiple services (evaluates all subdirs)"
    required: false
  policy-path:
    description: "Relative path to the conformity policy YAML (single source of truth for rules)"
    required: false
    default: "policy/conformity-policy.yaml"

outputs:
  decision:
    description: "Overall conformance decision: pass | manual_review | block"
  confidence:
    description: "Confidence score (0.0 – 1.0)"
  violations:
    description: "JSON array of all violations found"

runs:
  using: "composite"
  steps:
    - name: Setup Node.js 20
      uses: actions/setup-node@v4
      with:
        node-version: "20"

    - name: Install conformance agent dependencies
      shell: bash
      working-directory: ${{ github.action_path }}
      run: npm install

    - name: Install OpenCode CLI (required by createOpencode)
      shell: bash
      run: npm install -g opencode-ai

    - name: Run conformance gate
      id: gate
      shell: bash
      working-directory: ${{ github.workspace }}
      env:
        ANTHROPIC_API_KEY: ${{ env.ANTHROPIC_API_KEY }}
        INPUT_SERVICE_PATH: ${{ inputs.service-path }}
        INPUT_SERVICES_DIR: ${{ inputs.services-dir }}
        INPUT_POLICY_PATH: ${{ inputs.policy-path }}
      run: |
        set +e
        OUTPUT=$(${{ github.action_path }}/node_modules/.bin/tsx \
          ${{ github.action_path }}/src/index.ts 2>/dev/fd/2)
        EXIT_CODE=$?

        DECISION=$(echo "$OUTPUT" | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{ try{ const r=JSON.parse(d); process.stdout.write(r.decision||'block') } catch(e){ process.stdout.write('block') } })")
        CONFIDENCE=$(echo "$OUTPUT" | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{ try{ const r=JSON.parse(d); process.stdout.write(String(r.confidence||0)) } catch(e){ process.stdout.write('0') } })")
        VIOLATIONS=$(echo "$OUTPUT" | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{ try{ const r=JSON.parse(d); process.stdout.write(JSON.stringify(r.violations||[])) } catch(e){ process.stdout.write('[]') } })")

        echo "decision=$DECISION"     >> "$GITHUB_OUTPUT"
        echo "confidence=$CONFIDENCE" >> "$GITHUB_OUTPUT"
        echo "violations=$VIOLATIONS" >> "$GITHUB_OUTPUT"

        exit $EXIT_CODE
